#!/usr/bin/env lua
package.path = table.concat({
  "lua/?.lua",
  "lua/?/init.lua",
  package.path,
}, ";")
local engine = require("mutate4lua")
local handle = assert(io.popen("pwd", "r"))
local cwd = handle:read("*l")
handle:close()
os.exit(engine.run(arg, cwd, io.stdout, io.stderr))
